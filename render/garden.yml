module:
  name: render
  description: Receives mesh, returns PNG.
  type: container
  dockerfile: Dockerfile
  services:
    - name: render
      ports:
        - name: http
          containerPort: 8080
      ingresses:
        - path: /render
          port: http
