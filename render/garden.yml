module:
  name: render
  description: Receives mesh, returns PNG.
  type: container
  dockerfile: Dockerfile
  hotReload:
    sync:
      - target: /go/src/app
  services:
    - name: render
      ports:
        - name: http
          containerPort: 8080
      ingresses:
        - path: /render
          port: http
