module:
  name: mesh
  description: Generates vector and mesh.
  type: container
  dockerfile: Dockerfile
  hotReload:
    sync:
      - target: /go/src/app
  services:
    - name: mesh
      ports:
        - name: http
          containerPort: 8080
      ingresses:
        - path: /mesh
          port: http
